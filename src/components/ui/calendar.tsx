export function Calendar({ className = "" }: { className?: string }) {
  return (
    <div className={`${className} p-4 border rounded-md text-sm text-gray-500`}>
      [Calendar placeholder — plug in a real calendar later]
    </div>
  );
}